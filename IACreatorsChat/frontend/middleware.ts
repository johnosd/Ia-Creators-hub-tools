// (Opcional) Middleware para checar cookies no edge. Mantido mínimo nesta demo.
export function middleware() { /* implementar se desejar validação no edge */ }
export const config = { matcher: ["/chat"] };
